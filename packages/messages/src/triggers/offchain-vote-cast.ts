export const offchainVoteCastMessages = {
  withReason: `🗳️ {{address}} voted on Snapshot proposal in {{daoId}}: "{{proposalTitle}}"

Reason: "{{reason}}"`,

  withoutReason: `🗳️ {{address}} voted on Snapshot proposal in {{daoId}}: "{{proposalTitle}}"`
};
