schema: '${ANTICAPTURE_GRAPHQL_ENDPOINT}'
documents: 'queries/*.graphql'
generates:
  src/gql/:
    preset: 'client-preset'
    config:
      scalars:
        BigInt: string
      useTypeImports: true