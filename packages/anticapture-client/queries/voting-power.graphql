query ListVotingPowerHistorys($where: votingPowerHistoryFilter, $limit: Int, $orderBy: String, $orderDirection: String) {
  votingPowerHistorys(where: $where, limit: $limit, orderBy: $orderBy, orderDirection: $orderDirection) {
    items {
      accountId
      timestamp
      votingPower
      delta
      daoId
      transactionHash
      delegation {
        delegatorAccountId
        delegateAccountId
        delegatedValue
        previousDelegate
      }
      transfer {
        amount
        fromAccountId
        toAccountId
      }
    }
  }
}