[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/subscription-server/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 3

[environments.pr.deploy]
startCommand = "echo 'NOOP'"
healthcheckPath = ""
preDeployCommand = ""
