# Notification Activity per DAO Plan

1. Add a metrics query to aggregate notifications by DAO.
2. Expose the data via a dedicated API route.
3. Render a new chart on the dashboard.
4. Add a unit test for the new metrics query helper.
